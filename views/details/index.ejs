<% layout ('layouts/boilerplate') %> 
    <div class="row">
        <div class="col-6 offset-3">
            <h3>Names:</h3>
                <ul>
                    <% for(let detail of details) { %> 
                    <li><a href="/details/<%= detail._id %>"><%= detail.name %> </a></li>
                        <% } %> 
                </ul>
    <a class="btn btn-primary" href="/details/new">Add New</a>
        </div>
    </div>